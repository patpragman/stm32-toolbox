#ifndef HAL_DELAY_H
#define HAL_DELAY_H

#include <stdint.h>

/* Approximate millisecond delay based on the pack system clock. */
void hal_delay_ms(uint32_t ms);

static inline void delay(uint32_t ms)
{
    hal_delay_ms(ms);
}

#endif
